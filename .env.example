# Copy this file to .env and adjust values as needed.
# Docker Compose automatically reads .env from the project root.

# Port exposed on the host machine (change if 8099 is already in use)
APP_PORT=8099
